language: node_js
node_js: 12.16.3
sudo: true

addons:
  chrome: stable

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - ng test
  - yarn build --prod --base-href $base_href
