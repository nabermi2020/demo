language: node_js
node_js: 12.16.3
sudo: true

addons:
  firefox: latest
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
      - google-chrome-beta

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - ng test
